import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: ' indigo-Web-App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
